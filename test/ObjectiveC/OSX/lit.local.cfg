# -*- Python -*-

# Configuration file for the 'lit' test runner.

if 'darwin' in config.target_triple:
  if not lit_config.quiet:
    lit_config.note('Running Cocoa tests')
else:
  config.unsupported = True
